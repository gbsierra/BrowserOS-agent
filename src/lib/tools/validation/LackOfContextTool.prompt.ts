export const lackOfContextToolDescription = `Pause execution and request user input when the agent is blocked, uncertain, lacks required context, instructions are ambiguous, or continuing would be unsafe/not meaningful.

Return a structured JSON result; the orchestrator will pause silently and wait for user input. Provide a concise user-facing message and, when possible, list the specific information needed. You may include suggested prompts to help the user respond quickly.`


